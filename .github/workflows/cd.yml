name: CD

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deployment step
        run: echo "CD pipeline ready — add deployment steps here when hosting is configured."
